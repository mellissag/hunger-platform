# ================================================================
#  Hunger Beauty Platform — .env
#  Скопируй этот файл: cp .env.example .env
#  Заполни все значения. НИКОГДА не коммить .env в git.
# ================================================================

# ── Telegram ────────────────────────────────────────────────────
# Получить у @BotFather → /newbot
TELEGRAM_BOT_TOKEN=8753142473:AAFZv8Ssx6dYp5fOnWtTTrZZxPYvYtx_LwU

# Любая случайная строка 32+ символа
# Генерация: python3 -c "import secrets; print(secrets.token_hex(32))"
TELEGRAM_WEBHOOK_SECRET=f0cb0f18af76b870381268a4098225c6

# ── Домен ───────────────────────────────────────────────────────
APP_DOMAIN=test-adm.tech
APP_ENV=production
DEBUG=false

# ── База данных ─────────────────────────────────────────────────
POSTGRES_DB=hunger
POSTGRES_USER=hunger
POSTGRES_PASSWORD=a5802060c41a0a9a21837a600bbca64d

# Автособирается из параметров выше — менять не нужно
DATABASE_URL=postgresql+asyncpg://hunger:${POSTGRES_PASSWORD}@postgres:5432/hunger

# ── Redis ───────────────────────────────────────────────────────
REDIS_URL=redis://redis:6379/0

# ── Безопасность ────────────────────────────────────────────────
# Два разных ключа, каждый 64 символа
# Генерация: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=e3e2af447337a24d78711f04c17bb09155a8fe4129cf44dd7e17d2d4526d3902
JWT_SECRET=153289bf29ab53c5acd645e51629007c9aab8ebd3937ffa6abf0c241a291aae7

# ── Google Gemini AI ────────────────────────────────────────────
# Получить: https://aistudio.google.com/app/apikey
# Бесплатный тариф: 15 запросов/мин, 1500/день
GEMINI_API_KEY=

# ── Seed: начальные аккаунты ────────────────────────────────────
# Пароль для всех 4 аккаунтов при первом запуске seed_init.py
# После первого входа — смени через панель «Сотрудники»
SEED_PASSWORD=Mellissag?!@#

# ── Email / SMTP (опционально) ──────────────────────────────────
# Нужен только для отправки инвайтов сотрудникам на почту
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
